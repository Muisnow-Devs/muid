// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"sanzi.io/muid/internal/authz/ent/oidcclient"
	"sanzi.io/muid/internal/authz/ent/oidcclientsecret"
	"sanzi.io/muid/internal/authz/ent/predicate"
)

// OIDCClientSecretQuery is the builder for querying OIDCClientSecret entities.
type OIDCClientSecretQuery struct {
	config
	ctx        *QueryContext
	order      []oidcclientsecret.OrderOption
	inters     []Interceptor
	predicates []predicate.OIDCClientSecret
	withClient *OIDCClientQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OIDCClientSecretQuery builder.
func (_q *OIDCClientSecretQuery) Where(ps ...predicate.OIDCClientSecret) *OIDCClientSecretQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *OIDCClientSecretQuery) Limit(limit int) *OIDCClientSecretQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *OIDCClientSecretQuery) Offset(offset int) *OIDCClientSecretQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *OIDCClientSecretQuery) Unique(unique bool) *OIDCClientSecretQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *OIDCClientSecretQuery) Order(o ...oidcclientsecret.OrderOption) *OIDCClientSecretQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryClient chains the current query on the "client" edge.
func (_q *OIDCClientSecretQuery) QueryClient() *OIDCClientQuery {
	query := (&OIDCClientClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oidcclientsecret.Table, oidcclientsecret.FieldID, selector),
			sqlgraph.To(oidcclient.Table, oidcclient.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, oidcclientsecret.ClientTable, oidcclientsecret.ClientColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OIDCClientSecret entity from the query.
// Returns a *NotFoundError when no OIDCClientSecret was found.
func (_q *OIDCClientSecretQuery) First(ctx context.Context) (*OIDCClientSecret, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{oidcclientsecret.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *OIDCClientSecretQuery) FirstX(ctx context.Context) *OIDCClientSecret {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OIDCClientSecret ID from the query.
// Returns a *NotFoundError when no OIDCClientSecret ID was found.
func (_q *OIDCClientSecretQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{oidcclientsecret.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *OIDCClientSecretQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OIDCClientSecret entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OIDCClientSecret entity is found.
// Returns a *NotFoundError when no OIDCClientSecret entities are found.
func (_q *OIDCClientSecretQuery) Only(ctx context.Context) (*OIDCClientSecret, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{oidcclientsecret.Label}
	default:
		return nil, &NotSingularError{oidcclientsecret.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *OIDCClientSecretQuery) OnlyX(ctx context.Context) *OIDCClientSecret {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OIDCClientSecret ID in the query.
// Returns a *NotSingularError when more than one OIDCClientSecret ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *OIDCClientSecretQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{oidcclientsecret.Label}
	default:
		err = &NotSingularError{oidcclientsecret.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *OIDCClientSecretQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OIDCClientSecrets.
func (_q *OIDCClientSecretQuery) All(ctx context.Context) ([]*OIDCClientSecret, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OIDCClientSecret, *OIDCClientSecretQuery]()
	return withInterceptors[[]*OIDCClientSecret](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *OIDCClientSecretQuery) AllX(ctx context.Context) []*OIDCClientSecret {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OIDCClientSecret IDs.
func (_q *OIDCClientSecretQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(oidcclientsecret.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *OIDCClientSecretQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *OIDCClientSecretQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*OIDCClientSecretQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *OIDCClientSecretQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *OIDCClientSecretQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *OIDCClientSecretQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OIDCClientSecretQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *OIDCClientSecretQuery) Clone() *OIDCClientSecretQuery {
	if _q == nil {
		return nil
	}
	return &OIDCClientSecretQuery{
		config:     _q.config,
		ctx:        _q.ctx.Clone(),
		order:      append([]oidcclientsecret.OrderOption{}, _q.order...),
		inters:     append([]Interceptor{}, _q.inters...),
		predicates: append([]predicate.OIDCClientSecret{}, _q.predicates...),
		withClient: _q.withClient.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithClient tells the query-builder to eager-load the nodes that are connected to
// the "client" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OIDCClientSecretQuery) WithClient(opts ...func(*OIDCClientQuery)) *OIDCClientSecretQuery {
	query := (&OIDCClientClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withClient = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ClientRefID uuid.UUID `json:"client_ref_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OIDCClientSecret.Query().
//		GroupBy(oidcclientsecret.FieldClientRefID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *OIDCClientSecretQuery) GroupBy(field string, fields ...string) *OIDCClientSecretGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OIDCClientSecretGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = oidcclientsecret.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ClientRefID uuid.UUID `json:"client_ref_id,omitempty"`
//	}
//
//	client.OIDCClientSecret.Query().
//		Select(oidcclientsecret.FieldClientRefID).
//		Scan(ctx, &v)
func (_q *OIDCClientSecretQuery) Select(fields ...string) *OIDCClientSecretSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &OIDCClientSecretSelect{OIDCClientSecretQuery: _q}
	sbuild.label = oidcclientsecret.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OIDCClientSecretSelect configured with the given aggregations.
func (_q *OIDCClientSecretQuery) Aggregate(fns ...AggregateFunc) *OIDCClientSecretSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *OIDCClientSecretQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !oidcclientsecret.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *OIDCClientSecretQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OIDCClientSecret, error) {
	var (
		nodes       = []*OIDCClientSecret{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withClient != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OIDCClientSecret).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OIDCClientSecret{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withClient; query != nil {
		if err := _q.loadClient(ctx, query, nodes, nil,
			func(n *OIDCClientSecret, e *OIDCClient) { n.Edges.Client = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *OIDCClientSecretQuery) loadClient(ctx context.Context, query *OIDCClientQuery, nodes []*OIDCClientSecret, init func(*OIDCClientSecret), assign func(*OIDCClientSecret, *OIDCClient)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*OIDCClientSecret)
	for i := range nodes {
		fk := nodes[i].ClientRefID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(oidcclient.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "client_ref_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *OIDCClientSecretQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *OIDCClientSecretQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(oidcclientsecret.Table, oidcclientsecret.Columns, sqlgraph.NewFieldSpec(oidcclientsecret.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, oidcclientsecret.FieldID)
		for i := range fields {
			if fields[i] != oidcclientsecret.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withClient != nil {
			_spec.Node.AddColumnOnce(oidcclientsecret.FieldClientRefID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *OIDCClientSecretQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(oidcclientsecret.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = oidcclientsecret.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// OIDCClientSecretGroupBy is the group-by builder for OIDCClientSecret entities.
type OIDCClientSecretGroupBy struct {
	selector
	build *OIDCClientSecretQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *OIDCClientSecretGroupBy) Aggregate(fns ...AggregateFunc) *OIDCClientSecretGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *OIDCClientSecretGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OIDCClientSecretQuery, *OIDCClientSecretGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *OIDCClientSecretGroupBy) sqlScan(ctx context.Context, root *OIDCClientSecretQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OIDCClientSecretSelect is the builder for selecting fields of OIDCClientSecret entities.
type OIDCClientSecretSelect struct {
	*OIDCClientSecretQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *OIDCClientSecretSelect) Aggregate(fns ...AggregateFunc) *OIDCClientSecretSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *OIDCClientSecretSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OIDCClientSecretQuery, *OIDCClientSecretSelect](ctx, _s.OIDCClientSecretQuery, _s, _s.inters, v)
}

func (_s *OIDCClientSecretSelect) sqlScan(ctx context.Context, root *OIDCClientSecretQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
