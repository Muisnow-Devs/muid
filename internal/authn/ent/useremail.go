// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"sanzi.io/muid/internal/authn/ent/useremail"
	"sanzi.io/muid/internal/authn/ent/useridentity"
	"sanzi.io/muid/internal/authn/ent/userref"
)

// UserEmail is the model entity for the UserEmail schema.
type UserEmail struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// IdentityID holds the value of the "identity_id" field.
	IdentityID uuid.UUID `json:"identity_id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID uuid.UUID `json:"user_id,omitempty"`
	// Email holds the value of the "email" field.
	Email string `json:"email,omitempty"`
	// IsPrimary holds the value of the "is_primary" field.
	IsPrimary bool `json:"is_primary,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// RevokedAt holds the value of the "revoked_at" field.
	RevokedAt time.Time `json:"revoked_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserEmailQuery when eager-loading is set.
	Edges        UserEmailEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserEmailEdges holds the relations/edges for other nodes in the graph.
type UserEmailEdges struct {
	// User holds the value of the user edge.
	User *UserRef `json:"user,omitempty"`
	// Identity holds the value of the identity edge.
	Identity *UserIdentity `json:"identity,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserEmailEdges) UserOrErr() (*UserRef, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: userref.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// IdentityOrErr returns the Identity value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserEmailEdges) IdentityOrErr() (*UserIdentity, error) {
	if e.Identity != nil {
		return e.Identity, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: useridentity.Label}
	}
	return nil, &NotLoadedError{edge: "identity"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserEmail) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case useremail.FieldIsPrimary:
			values[i] = new(sql.NullBool)
		case useremail.FieldEmail:
			values[i] = new(sql.NullString)
		case useremail.FieldCreatedAt, useremail.FieldUpdatedAt, useremail.FieldRevokedAt:
			values[i] = new(sql.NullTime)
		case useremail.FieldID, useremail.FieldIdentityID, useremail.FieldUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserEmail fields.
func (_m *UserEmail) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case useremail.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case useremail.FieldIdentityID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field identity_id", values[i])
			} else if value != nil {
				_m.IdentityID = *value
			}
		case useremail.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				_m.UserID = *value
			}
		case useremail.FieldEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email", values[i])
			} else if value.Valid {
				_m.Email = value.String
			}
		case useremail.FieldIsPrimary:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_primary", values[i])
			} else if value.Valid {
				_m.IsPrimary = value.Bool
			}
		case useremail.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case useremail.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case useremail.FieldRevokedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field revoked_at", values[i])
			} else if value.Valid {
				_m.RevokedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserEmail.
// This includes values selected through modifiers, order, etc.
func (_m *UserEmail) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserEmail entity.
func (_m *UserEmail) QueryUser() *UserRefQuery {
	return NewUserEmailClient(_m.config).QueryUser(_m)
}

// QueryIdentity queries the "identity" edge of the UserEmail entity.
func (_m *UserEmail) QueryIdentity() *UserIdentityQuery {
	return NewUserEmailClient(_m.config).QueryIdentity(_m)
}

// Update returns a builder for updating this UserEmail.
// Note that you need to call UserEmail.Unwrap() before calling this method if this UserEmail
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserEmail) Update() *UserEmailUpdateOne {
	return NewUserEmailClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserEmail entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserEmail) Unwrap() *UserEmail {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserEmail is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserEmail) String() string {
	var builder strings.Builder
	builder.WriteString("UserEmail(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("identity_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.IdentityID))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("email=")
	builder.WriteString(_m.Email)
	builder.WriteString(", ")
	builder.WriteString("is_primary=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsPrimary))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("revoked_at=")
	builder.WriteString(_m.RevokedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserEmails is a parsable slice of UserEmail.
type UserEmails []*UserEmail
