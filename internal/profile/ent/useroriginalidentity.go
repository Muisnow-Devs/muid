// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"sanzi.io/muid/internal/profile/ent/useroriginalidentity"
	"sanzi.io/muid/internal/profile/ent/userprofile"
)

// UserOriginalIdentity is the model entity for the UserOriginalIdentity schema.
type UserOriginalIdentity struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// OriginalIdentity holds the value of the "original_identity" field.
	OriginalIdentity string `json:"original_identity,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserOriginalIdentityQuery when eager-loading is set.
	Edges                       UserOriginalIdentityEdges `json:"edges"`
	user_original_identity_user *uuid.UUID
	selectValues                sql.SelectValues
}

// UserOriginalIdentityEdges holds the relations/edges for other nodes in the graph.
type UserOriginalIdentityEdges struct {
	// User holds the value of the user edge.
	User *UserProfile `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserOriginalIdentityEdges) UserOrErr() (*UserProfile, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: userprofile.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserOriginalIdentity) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case useroriginalidentity.FieldID:
			values[i] = new(sql.NullInt64)
		case useroriginalidentity.FieldOriginalIdentity:
			values[i] = new(sql.NullString)
		case useroriginalidentity.ForeignKeys[0]: // user_original_identity_user
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserOriginalIdentity fields.
func (_m *UserOriginalIdentity) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case useroriginalidentity.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case useroriginalidentity.FieldOriginalIdentity:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field original_identity", values[i])
			} else if value.Valid {
				_m.OriginalIdentity = value.String
			}
		case useroriginalidentity.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field user_original_identity_user", values[i])
			} else if value.Valid {
				_m.user_original_identity_user = new(uuid.UUID)
				*_m.user_original_identity_user = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserOriginalIdentity.
// This includes values selected through modifiers, order, etc.
func (_m *UserOriginalIdentity) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserOriginalIdentity entity.
func (_m *UserOriginalIdentity) QueryUser() *UserProfileQuery {
	return NewUserOriginalIdentityClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this UserOriginalIdentity.
// Note that you need to call UserOriginalIdentity.Unwrap() before calling this method if this UserOriginalIdentity
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserOriginalIdentity) Update() *UserOriginalIdentityUpdateOne {
	return NewUserOriginalIdentityClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserOriginalIdentity entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserOriginalIdentity) Unwrap() *UserOriginalIdentity {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserOriginalIdentity is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserOriginalIdentity) String() string {
	var builder strings.Builder
	builder.WriteString("UserOriginalIdentity(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("original_identity=")
	builder.WriteString(_m.OriginalIdentity)
	builder.WriteByte(')')
	return builder.String()
}

// UserOriginalIdentities is a parsable slice of UserOriginalIdentity.
type UserOriginalIdentities []*UserOriginalIdentity
