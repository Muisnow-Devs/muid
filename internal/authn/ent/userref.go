// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"sanzi.io/muid/internal/authn/ent/userref"
)

// UserRef is the model entity for the UserRef schema.
type UserRef struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Username holds the value of the "username" field.
	Username string `json:"username,omitempty"`
	// Email holds the value of the "email" field.
	Email string `json:"email,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserRefQuery when eager-loading is set.
	Edges        UserRefEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserRefEdges holds the relations/edges for other nodes in the graph.
type UserRefEdges struct {
	// Sessions holds the value of the sessions edge.
	Sessions []*UserSession `json:"sessions,omitempty"`
	// Passkeys holds the value of the passkeys edge.
	Passkeys []*UserPasskey `json:"passkeys,omitempty"`
	// OidcRefreshTokens holds the value of the oidc_refresh_tokens edge.
	OidcRefreshTokens []*OIDCRefreshToken `json:"oidc_refresh_tokens,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// SessionsOrErr returns the Sessions value or an error if the edge
// was not loaded in eager-loading.
func (e UserRefEdges) SessionsOrErr() ([]*UserSession, error) {
	if e.loadedTypes[0] {
		return e.Sessions, nil
	}
	return nil, &NotLoadedError{edge: "sessions"}
}

// PasskeysOrErr returns the Passkeys value or an error if the edge
// was not loaded in eager-loading.
func (e UserRefEdges) PasskeysOrErr() ([]*UserPasskey, error) {
	if e.loadedTypes[1] {
		return e.Passkeys, nil
	}
	return nil, &NotLoadedError{edge: "passkeys"}
}

// OidcRefreshTokensOrErr returns the OidcRefreshTokens value or an error if the edge
// was not loaded in eager-loading.
func (e UserRefEdges) OidcRefreshTokensOrErr() ([]*OIDCRefreshToken, error) {
	if e.loadedTypes[2] {
		return e.OidcRefreshTokens, nil
	}
	return nil, &NotLoadedError{edge: "oidc_refresh_tokens"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserRef) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userref.FieldUsername, userref.FieldEmail:
			values[i] = new(sql.NullString)
		case userref.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserRef fields.
func (_m *UserRef) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userref.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case userref.FieldUsername:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field username", values[i])
			} else if value.Valid {
				_m.Username = value.String
			}
		case userref.FieldEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email", values[i])
			} else if value.Valid {
				_m.Email = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserRef.
// This includes values selected through modifiers, order, etc.
func (_m *UserRef) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySessions queries the "sessions" edge of the UserRef entity.
func (_m *UserRef) QuerySessions() *UserSessionQuery {
	return NewUserRefClient(_m.config).QuerySessions(_m)
}

// QueryPasskeys queries the "passkeys" edge of the UserRef entity.
func (_m *UserRef) QueryPasskeys() *UserPasskeyQuery {
	return NewUserRefClient(_m.config).QueryPasskeys(_m)
}

// QueryOidcRefreshTokens queries the "oidc_refresh_tokens" edge of the UserRef entity.
func (_m *UserRef) QueryOidcRefreshTokens() *OIDCRefreshTokenQuery {
	return NewUserRefClient(_m.config).QueryOidcRefreshTokens(_m)
}

// Update returns a builder for updating this UserRef.
// Note that you need to call UserRef.Unwrap() before calling this method if this UserRef
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserRef) Update() *UserRefUpdateOne {
	return NewUserRefClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserRef entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserRef) Unwrap() *UserRef {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserRef is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserRef) String() string {
	var builder strings.Builder
	builder.WriteString("UserRef(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("username=")
	builder.WriteString(_m.Username)
	builder.WriteString(", ")
	builder.WriteString("email=")
	builder.WriteString(_m.Email)
	builder.WriteByte(')')
	return builder.String()
}

// UserRefs is a parsable slice of UserRef.
type UserRefs []*UserRef
