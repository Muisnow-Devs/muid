syntax = "proto3";

package muid.authn.v1;

option go_package = "sanzi.io/muid/api/proto/authn/v1;authn";

// Authn's public surface is split into the focused services declared in the
// sibling proto files. This package marker remains as the stable module entry.
