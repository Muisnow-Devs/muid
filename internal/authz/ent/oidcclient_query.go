// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"sanzi.io/muid/internal/authz/ent/oidccallbackuri"
	"sanzi.io/muid/internal/authz/ent/oidcclient"
	"sanzi.io/muid/internal/authz/ent/oidcclientsecret"
	"sanzi.io/muid/internal/authz/ent/oidcgrant"
	"sanzi.io/muid/internal/authz/ent/oidcrefreshtoken"
	"sanzi.io/muid/internal/authz/ent/organization"
	"sanzi.io/muid/internal/authz/ent/predicate"
)

// OIDCClientQuery is the builder for querying OIDCClient entities.
type OIDCClientQuery struct {
	config
	ctx               *QueryContext
	order             []oidcclient.OrderOption
	inters            []Interceptor
	predicates        []predicate.OIDCClient
	withCallbackUrls  *OIDCCallbackURIQuery
	withSecrets       *OIDCClientSecretQuery
	withGrants        *OIDCGrantQuery
	withRefreshTokens *OIDCRefreshTokenQuery
	withOrganization  *OrganizationQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OIDCClientQuery builder.
func (_q *OIDCClientQuery) Where(ps ...predicate.OIDCClient) *OIDCClientQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *OIDCClientQuery) Limit(limit int) *OIDCClientQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *OIDCClientQuery) Offset(offset int) *OIDCClientQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *OIDCClientQuery) Unique(unique bool) *OIDCClientQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *OIDCClientQuery) Order(o ...oidcclient.OrderOption) *OIDCClientQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryCallbackUrls chains the current query on the "callback_urls" edge.
func (_q *OIDCClientQuery) QueryCallbackUrls() *OIDCCallbackURIQuery {
	query := (&OIDCCallbackURIClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oidcclient.Table, oidcclient.FieldID, selector),
			sqlgraph.To(oidccallbackuri.Table, oidccallbackuri.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, oidcclient.CallbackUrlsTable, oidcclient.CallbackUrlsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySecrets chains the current query on the "secrets" edge.
func (_q *OIDCClientQuery) QuerySecrets() *OIDCClientSecretQuery {
	query := (&OIDCClientSecretClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oidcclient.Table, oidcclient.FieldID, selector),
			sqlgraph.To(oidcclientsecret.Table, oidcclientsecret.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, oidcclient.SecretsTable, oidcclient.SecretsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGrants chains the current query on the "grants" edge.
func (_q *OIDCClientQuery) QueryGrants() *OIDCGrantQuery {
	query := (&OIDCGrantClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oidcclient.Table, oidcclient.FieldID, selector),
			sqlgraph.To(oidcgrant.Table, oidcgrant.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, oidcclient.GrantsTable, oidcclient.GrantsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRefreshTokens chains the current query on the "refresh_tokens" edge.
func (_q *OIDCClientQuery) QueryRefreshTokens() *OIDCRefreshTokenQuery {
	query := (&OIDCRefreshTokenClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oidcclient.Table, oidcclient.FieldID, selector),
			sqlgraph.To(oidcrefreshtoken.Table, oidcrefreshtoken.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, oidcclient.RefreshTokensTable, oidcclient.RefreshTokensColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrganization chains the current query on the "organization" edge.
func (_q *OIDCClientQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oidcclient.Table, oidcclient.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, oidcclient.OrganizationTable, oidcclient.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OIDCClient entity from the query.
// Returns a *NotFoundError when no OIDCClient was found.
func (_q *OIDCClientQuery) First(ctx context.Context) (*OIDCClient, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{oidcclient.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *OIDCClientQuery) FirstX(ctx context.Context) *OIDCClient {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OIDCClient ID from the query.
// Returns a *NotFoundError when no OIDCClient ID was found.
func (_q *OIDCClientQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{oidcclient.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *OIDCClientQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OIDCClient entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OIDCClient entity is found.
// Returns a *NotFoundError when no OIDCClient entities are found.
func (_q *OIDCClientQuery) Only(ctx context.Context) (*OIDCClient, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{oidcclient.Label}
	default:
		return nil, &NotSingularError{oidcclient.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *OIDCClientQuery) OnlyX(ctx context.Context) *OIDCClient {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OIDCClient ID in the query.
// Returns a *NotSingularError when more than one OIDCClient ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *OIDCClientQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{oidcclient.Label}
	default:
		err = &NotSingularError{oidcclient.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *OIDCClientQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OIDCClients.
func (_q *OIDCClientQuery) All(ctx context.Context) ([]*OIDCClient, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OIDCClient, *OIDCClientQuery]()
	return withInterceptors[[]*OIDCClient](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *OIDCClientQuery) AllX(ctx context.Context) []*OIDCClient {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OIDCClient IDs.
func (_q *OIDCClientQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(oidcclient.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *OIDCClientQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *OIDCClientQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*OIDCClientQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *OIDCClientQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *OIDCClientQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *OIDCClientQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OIDCClientQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *OIDCClientQuery) Clone() *OIDCClientQuery {
	if _q == nil {
		return nil
	}
	return &OIDCClientQuery{
		config:            _q.config,
		ctx:               _q.ctx.Clone(),
		order:             append([]oidcclient.OrderOption{}, _q.order...),
		inters:            append([]Interceptor{}, _q.inters...),
		predicates:        append([]predicate.OIDCClient{}, _q.predicates...),
		withCallbackUrls:  _q.withCallbackUrls.Clone(),
		withSecrets:       _q.withSecrets.Clone(),
		withGrants:        _q.withGrants.Clone(),
		withRefreshTokens: _q.withRefreshTokens.Clone(),
		withOrganization:  _q.withOrganization.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithCallbackUrls tells the query-builder to eager-load the nodes that are connected to
// the "callback_urls" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OIDCClientQuery) WithCallbackUrls(opts ...func(*OIDCCallbackURIQuery)) *OIDCClientQuery {
	query := (&OIDCCallbackURIClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCallbackUrls = query
	return _q
}

// WithSecrets tells the query-builder to eager-load the nodes that are connected to
// the "secrets" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OIDCClientQuery) WithSecrets(opts ...func(*OIDCClientSecretQuery)) *OIDCClientQuery {
	query := (&OIDCClientSecretClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSecrets = query
	return _q
}

// WithGrants tells the query-builder to eager-load the nodes that are connected to
// the "grants" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OIDCClientQuery) WithGrants(opts ...func(*OIDCGrantQuery)) *OIDCClientQuery {
	query := (&OIDCGrantClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withGrants = query
	return _q
}

// WithRefreshTokens tells the query-builder to eager-load the nodes that are connected to
// the "refresh_tokens" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OIDCClientQuery) WithRefreshTokens(opts ...func(*OIDCRefreshTokenQuery)) *OIDCClientQuery {
	query := (&OIDCRefreshTokenClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRefreshTokens = query
	return _q
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OIDCClientQuery) WithOrganization(opts ...func(*OrganizationQuery)) *OIDCClientQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOrganization = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ClientID string `json:"client_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OIDCClient.Query().
//		GroupBy(oidcclient.FieldClientID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *OIDCClientQuery) GroupBy(field string, fields ...string) *OIDCClientGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OIDCClientGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = oidcclient.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ClientID string `json:"client_id,omitempty"`
//	}
//
//	client.OIDCClient.Query().
//		Select(oidcclient.FieldClientID).
//		Scan(ctx, &v)
func (_q *OIDCClientQuery) Select(fields ...string) *OIDCClientSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &OIDCClientSelect{OIDCClientQuery: _q}
	sbuild.label = oidcclient.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OIDCClientSelect configured with the given aggregations.
func (_q *OIDCClientQuery) Aggregate(fns ...AggregateFunc) *OIDCClientSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *OIDCClientQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !oidcclient.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *OIDCClientQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OIDCClient, error) {
	var (
		nodes       = []*OIDCClient{}
		_spec       = _q.querySpec()
		loadedTypes = [5]bool{
			_q.withCallbackUrls != nil,
			_q.withSecrets != nil,
			_q.withGrants != nil,
			_q.withRefreshTokens != nil,
			_q.withOrganization != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OIDCClient).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OIDCClient{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withCallbackUrls; query != nil {
		if err := _q.loadCallbackUrls(ctx, query, nodes,
			func(n *OIDCClient) { n.Edges.CallbackUrls = []*OIDCCallbackURI{} },
			func(n *OIDCClient, e *OIDCCallbackURI) { n.Edges.CallbackUrls = append(n.Edges.CallbackUrls, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSecrets; query != nil {
		if err := _q.loadSecrets(ctx, query, nodes,
			func(n *OIDCClient) { n.Edges.Secrets = []*OIDCClientSecret{} },
			func(n *OIDCClient, e *OIDCClientSecret) { n.Edges.Secrets = append(n.Edges.Secrets, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withGrants; query != nil {
		if err := _q.loadGrants(ctx, query, nodes,
			func(n *OIDCClient) { n.Edges.Grants = []*OIDCGrant{} },
			func(n *OIDCClient, e *OIDCGrant) { n.Edges.Grants = append(n.Edges.Grants, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRefreshTokens; query != nil {
		if err := _q.loadRefreshTokens(ctx, query, nodes,
			func(n *OIDCClient) { n.Edges.RefreshTokens = []*OIDCRefreshToken{} },
			func(n *OIDCClient, e *OIDCRefreshToken) { n.Edges.RefreshTokens = append(n.Edges.RefreshTokens, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withOrganization; query != nil {
		if err := _q.loadOrganization(ctx, query, nodes, nil,
			func(n *OIDCClient, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *OIDCClientQuery) loadCallbackUrls(ctx context.Context, query *OIDCCallbackURIQuery, nodes []*OIDCClient, init func(*OIDCClient), assign func(*OIDCClient, *OIDCCallbackURI)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*OIDCClient)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(oidccallbackuri.FieldClientRefID)
	}
	query.Where(predicate.OIDCCallbackURI(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(oidcclient.CallbackUrlsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ClientRefID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "client_ref_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *OIDCClientQuery) loadSecrets(ctx context.Context, query *OIDCClientSecretQuery, nodes []*OIDCClient, init func(*OIDCClient), assign func(*OIDCClient, *OIDCClientSecret)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*OIDCClient)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(oidcclientsecret.FieldClientRefID)
	}
	query.Where(predicate.OIDCClientSecret(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(oidcclient.SecretsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ClientRefID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "client_ref_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *OIDCClientQuery) loadGrants(ctx context.Context, query *OIDCGrantQuery, nodes []*OIDCClient, init func(*OIDCClient), assign func(*OIDCClient, *OIDCGrant)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*OIDCClient)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(oidcgrant.FieldClientRefID)
	}
	query.Where(predicate.OIDCGrant(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(oidcclient.GrantsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ClientRefID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "client_ref_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *OIDCClientQuery) loadRefreshTokens(ctx context.Context, query *OIDCRefreshTokenQuery, nodes []*OIDCClient, init func(*OIDCClient), assign func(*OIDCClient, *OIDCRefreshToken)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*OIDCClient)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(oidcrefreshtoken.FieldClientRefID)
	}
	query.Where(predicate.OIDCRefreshToken(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(oidcclient.RefreshTokensColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ClientRefID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "client_ref_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *OIDCClientQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*OIDCClient, init func(*OIDCClient), assign func(*OIDCClient, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*OIDCClient)
	for i := range nodes {
		fk := nodes[i].OwnerOrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "owner_organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *OIDCClientQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *OIDCClientQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(oidcclient.Table, oidcclient.Columns, sqlgraph.NewFieldSpec(oidcclient.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, oidcclient.FieldID)
		for i := range fields {
			if fields[i] != oidcclient.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withOrganization != nil {
			_spec.Node.AddColumnOnce(oidcclient.FieldOwnerOrganizationID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *OIDCClientQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(oidcclient.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = oidcclient.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// OIDCClientGroupBy is the group-by builder for OIDCClient entities.
type OIDCClientGroupBy struct {
	selector
	build *OIDCClientQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *OIDCClientGroupBy) Aggregate(fns ...AggregateFunc) *OIDCClientGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *OIDCClientGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OIDCClientQuery, *OIDCClientGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *OIDCClientGroupBy) sqlScan(ctx context.Context, root *OIDCClientQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OIDCClientSelect is the builder for selecting fields of OIDCClient entities.
type OIDCClientSelect struct {
	*OIDCClientQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *OIDCClientSelect) Aggregate(fns ...AggregateFunc) *OIDCClientSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *OIDCClientSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OIDCClientQuery, *OIDCClientSelect](ctx, _s.OIDCClientQuery, _s, _s.inters, v)
}

func (_s *OIDCClientSelect) sqlScan(ctx context.Context, root *OIDCClientQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
