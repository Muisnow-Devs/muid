// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"sanzi.io/muid/internal/authn/ent/oidcscope"
)

// OIDCScope is the model entity for the OIDCScope schema.
type OIDCScope struct {
	config `json:"-"`
	// ID of the ent.
	// Scope identifier used in OAuth 2.0 / OIDC requests, e.g. "organization:write".
	ID string `json:"id,omitempty"`
	// Short human-readable display name for the scope.
	Name string `json:"name,omitempty"`
	// i18n description map keyed by BCP-47 language tag (e.g. "en", "zh-TW").
	Description  map[string]string `json:"description,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*OIDCScope) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case oidcscope.FieldDescription:
			values[i] = new([]byte)
		case oidcscope.FieldID, oidcscope.FieldName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the OIDCScope fields.
func (_m *OIDCScope) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case oidcscope.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case oidcscope.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case oidcscope.FieldDescription:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Description); err != nil {
					return fmt.Errorf("unmarshal field description: %w", err)
				}
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the OIDCScope.
// This includes values selected through modifiers, order, etc.
func (_m *OIDCScope) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this OIDCScope.
// Note that you need to call OIDCScope.Unwrap() before calling this method if this OIDCScope
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *OIDCScope) Update() *OIDCScopeUpdateOne {
	return NewOIDCScopeClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the OIDCScope entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *OIDCScope) Unwrap() *OIDCScope {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: OIDCScope is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *OIDCScope) String() string {
	var builder strings.Builder
	builder.WriteString("OIDCScope(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(fmt.Sprintf("%v", _m.Description))
	builder.WriteByte(')')
	return builder.String()
}

// OIDCScopes is a parsable slice of OIDCScope.
type OIDCScopes []*OIDCScope
