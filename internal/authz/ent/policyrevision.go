// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"sanzi.io/muid/internal/authz/ent/policyrevision"
)

// PolicyRevision is the model entity for the PolicyRevision schema.
type PolicyRevision struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Revision holds the value of the "revision" field.
	Revision uuid.UUID `json:"revision,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PolicyRevision) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case policyrevision.FieldID:
			values[i] = new(sql.NullInt64)
		case policyrevision.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case policyrevision.FieldRevision:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PolicyRevision fields.
func (_m *PolicyRevision) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case policyrevision.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case policyrevision.FieldRevision:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field revision", values[i])
			} else if value != nil {
				_m.Revision = *value
			}
		case policyrevision.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PolicyRevision.
// This includes values selected through modifiers, order, etc.
func (_m *PolicyRevision) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this PolicyRevision.
// Note that you need to call PolicyRevision.Unwrap() before calling this method if this PolicyRevision
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *PolicyRevision) Update() *PolicyRevisionUpdateOne {
	return NewPolicyRevisionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the PolicyRevision entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *PolicyRevision) Unwrap() *PolicyRevision {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: PolicyRevision is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *PolicyRevision) String() string {
	var builder strings.Builder
	builder.WriteString("PolicyRevision(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("revision=")
	builder.WriteString(fmt.Sprintf("%v", _m.Revision))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PolicyRevisions is a parsable slice of PolicyRevision.
type PolicyRevisions []*PolicyRevision
